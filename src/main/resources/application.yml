spring:
  datasource:
    url: jdbc:mysql://localhost:3306/songHolder
    username: root
    password: 
    drive-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: mysql
    database_platform: org.hibernate.dialect.MySQL8Dialect

